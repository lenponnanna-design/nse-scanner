    - name: Debug secrets
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "BOT_TOKEN starts with: ${BOT_TOKEN:0:5}"
        echo "CHAT_ID: $CHAT_ID"

    - name: Run scanner
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        python scanner.py
